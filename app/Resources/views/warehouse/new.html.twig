{% extends 'base.html.twig' %}

{% block body %}
    <h1>Przyjmij Materiał</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('warehouse_index') }}"><button type="button">Stan Magazynowy</button></a>
            <a href="{{ path('Index') }}"><button type="button">Ekran Główny</button></a> 
        </li>
    </ul>
{% endblock %}
